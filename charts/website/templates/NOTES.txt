{{- if .Values.enabled }}
🎉 Account Frontend has been deployed!

Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
Release: {{ .Release.Name }}
Namespace: {{ include "website.namespace" . }}

📋 Deployment Information:
  - Replicas: {{ .Values.replicaCount }}
  - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  - Service: {{ include "website.fullname" . }}:{{ .Values.service.port }}

🌐 Access Information:
{{- if .Values.gateway.enabled }}
  Account App: https://{{ include "website.gateway.hostname" . }}

  Open in browser:
  open https://{{ include "website.gateway.hostname" . }}
{{- else }}
  Gateway is disabled. Use port-forward to access:
  kubectl port-forward -n {{ include "website.namespace" . }} svc/{{ include "website.fullname" . }} 8080:{{ .Values.service.port }}
  # Open http://localhost:8080
{{- end }}

🔍 Next Steps:
  1. Verify pods are running:
     kubectl get pods -n {{ include "website.namespace" . }} -l app.kubernetes.io/name={{ include "website.name" . }}

  2. Check logs:
     kubectl logs -n {{ include "website.namespace" . }} -l app.kubernetes.io/name={{ include "website.name" . }} --tail=50

  3. Test frontend:
     curl https://{{ include "website.gateway.hostname" . }}

📱 Frontend Features:
  - Static file serving (nginx)
  - Read-only root filesystem (security)
  - CDN-ready configuration
  - Environment variable injection

{{- else }}
Account Frontend is disabled (enabled: false)
{{- end }}
