{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "patient.fullname" . }}
  namespace: {{ include "patient.namespace" . }}
  labels:
    {{- include "patient.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "patient.gateway.name" . }}
      namespace: {{ include "patient.gateway.namespace" . }}
      sectionName: https  # Only attach to HTTPS listener
  hostnames:
    - {{ include "patient.gateway.hostname" . }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "patient.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}
