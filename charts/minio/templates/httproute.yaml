{{- if and .Values.enabled .Values.minio.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "minio.fullname" . }}
  namespace: {{ include "minio.namespace" . }}
  labels:
    {{- include "minio.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "minio.gateway.name" . }}
      namespace: {{ include "minio.gateway.namespace" . }}
      sectionName: https  # Only attach to HTTPS listener
  hostnames:
    - {{ include "minio.gateway.hostname" . }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: minio
          port: 9000
          weight: 1
---
{{- if .Values.minio.gateway.rateLimit.enabled }}
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: {{ include "minio.fullname" . }}-rate-limit
  namespace: {{ include "minio.namespace" . }}
  labels:
    {{- include "minio.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: {{ include "minio.fullname" . }}
  rateLimit:
    global:
      rules:
        # Per-IP rate limit
        - clientSelectors:
            - headers:
                - name: X-Forwarded-For
                  type: Distinct
          limit:
            requests: {{ .Values.minio.gateway.rateLimit.perIP.requests }}
            unit: {{ .Values.minio.gateway.rateLimit.perIP.unit }}
        # Burst limit
        - clientSelectors:
            - headers:
                - name: X-Forwarded-For
                  type: Distinct
          limit:
            requests: {{ .Values.minio.gateway.rateLimit.burst.requests }}
            unit: {{ .Values.minio.gateway.rateLimit.burst.unit }}
{{- end }}
{{- end }}
