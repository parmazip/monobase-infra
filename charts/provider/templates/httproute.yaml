{{- if and .Values.enabled .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "provider.fullname" . }}
  namespace: {{ include "provider.namespace" . }}
  labels:
    {{- include "provider.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "provider.gateway.name" . }}
      namespace: {{ include "provider.gateway.namespace" . }}
      sectionName: https  # Only attach to HTTPS listener
  hostnames:
    - {{ include "provider.gateway.hostname" . }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "provider.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 1
{{- end }}
