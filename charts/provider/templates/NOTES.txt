{{- if .Values.enabled }}
🎉 Account Frontend has been deployed!

Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
Release: {{ .Release.Name }}
Namespace: {{ include "provider.namespace" . }}

📋 Deployment Information:
  - Replicas: {{ .Values.replicaCount }}
  - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  - Service: {{ include "provider.fullname" . }}:{{ .Values.service.port }}

🌐 Access Information:
{{- if .Values.gateway.enabled }}
  Account App: https://{{ include "provider.gateway.hostname" . }}

  Open in browser:
  open https://{{ include "provider.gateway.hostname" . }}
{{- else }}
  Gateway is disabled. Use port-forward to access:
  kubectl port-forward -n {{ include "provider.namespace" . }} svc/{{ include "provider.fullname" . }} 8080:{{ .Values.service.port }}
  # Open http://localhost:8080
{{- end }}

🔍 Next Steps:
  1. Verify pods are running:
     kubectl get pods -n {{ include "provider.namespace" . }} -l app.kubernetes.io/name={{ include "provider.name" . }}

  2. Check logs:
     kubectl logs -n {{ include "provider.namespace" . }} -l app.kubernetes.io/name={{ include "provider.name" . }} --tail=50

  3. Test frontend:
     curl https://{{ include "provider.gateway.hostname" . }}

📱 Frontend Features:
  - Static file serving (nginx)
  - Read-only root filesystem (security)
  - CDN-ready configuration
  - Environment variable injection

{{- else }}
Account Frontend is disabled (enabled: false)
{{- end }}
